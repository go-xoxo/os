# Environment report

## Commands run

- `ls` output:
  - CHANGELOG.md
  - LICENSE
  - app
  - dist-newstyle
  - fourmolu.yaml
  - squirrel-os.cabal
  - src

- `find .. -name AGENTS.md -print` output: (no results)

## Notes

This report records the commands executed to answer the user question.
